export const STORAGE_KEYS = {
  FAVORITE_EVENTS: '@findlocal/favorite_events',
  PREFERRED_CITY: '@findlocal/preferred_city',
  FILTERS: '@findlocal/filters',
  THEME_MODE: '@findlocal/theme_mode',
} as const;
