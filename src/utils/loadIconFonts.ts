// Native (iOS/Android) no-op. Web has a .web.ts implementation that injects @font-face.
export function loadIconFonts() {
  // No-op on native platforms
}
/**
 * Native platform version - no-op
 * Icon fonts are loaded automatically on native platforms
 */
export function loadIconFonts() {
  // No-op for native platforms
}
